# Rename this file to secrets.toml and fill in your values.
# This file goes in the .streamlit/ folder (already in .gitignore — never commit it).
# On Streamlit Cloud: paste these values in the app settings under "Secrets".

SUPABASE_URL = "https://xxxxxxxxxxxx.supabase.co"
SUPABASE_KEY = "eyJhbGc..."
